import React from 'react';
import { RouteComponentProps } from '@reach/router';

const Route: React.FC<RouteComponentProps> = ({ children }) => {
  return <>{children}</>;
};

export default Route;
